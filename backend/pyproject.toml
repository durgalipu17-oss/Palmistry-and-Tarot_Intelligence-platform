[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi", "uvicorn[standard]", "sqlalchemy", "psycopg2-binary", "python-jose[cryptography]", "passlib[bcrypt]", "python-multipart", "pydantic", "google-genai", "python-dotenv", "pillow", "tensorflow", "numpy" ]
requires-python = "~=3.13.0"
